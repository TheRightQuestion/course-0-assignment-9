#include <iostream>
#include <string>
using namespace std;

int main() {
  int limit;
  cin >> limit;
  cout << "I am going to print out the first " << limit << " numbers using a loop" << endl;
  for (i = 0; i <= limit; i++) {
    cout << i << endl;
  }
  return 0;
}
